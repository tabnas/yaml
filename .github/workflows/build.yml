name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [24.x]

    runs-on: ${{ matrix.os }}

    steps:
    - name: Use LF line endings
      run: git config --global core.autocrlf false
    - uses: actions/checkout@v4
      with:
        path: yaml
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - name: Clone tabnas dependencies
      shell: bash
      run: |
        set -e
        for dep in parser debug json abnf railroad jsonic; do
          git clone --depth 1 "https://github.com/tabnas/$dep.git" "$dep"
        done
    - name: Build (dependencies, then this repo)
      shell: bash
      run: |
        set -e
        for pkg in parser debug json abnf railroad jsonic yaml; do
          ( cd "$pkg/ts" && npm i && npm run build --if-present )
        done
    - name: Test
      shell: bash
      working-directory: yaml/ts
      run: npm test
